<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/fragment_container"/>

    <!-- <TextView
         android:id="@+id/hello_world_txt"
         android:text="@string/hello_world"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:textSize="16sp"
         android:layout_marginBottom="20dp"/>

     <Spinner
         android:id="@+id/waypoint_dropdown"
         android:layout_width="match_parent"
         android:layout_height="wrap_content"
         android:layout_below="@id/hello_world_txt"
         android:layout_marginBottom="10dp"
         android:spinnerMode="dropdown"/>

     <Button
         android:id="@+id/upload_btn"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:padding="10dp"
         android:text="Upload"
         android:textSize="14sp"
         android:layout_below="@+id/waypoint_dropdown"
         android:background="#457894"/>

     <Button
         android:id="@+id/connect_btn"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:padding="10dp"
         android:text="@string/connect"
         android:textSize="14sp"
         android:layout_below="@id/upload_btn"
         android:layout_marginTop="20dp"
         android:background="#985637"/>

     <Button
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:id="@+id/guided_mode"
         android:text="Guided mode"
         android:padding="10dp"
         android:background="#849347"
         android:layout_below="@+id/connect_btn"
         android:layout_marginTop="20dp"/>

     <Button
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:id="@+id/auto_mode"
         android:text="Auto mode"
         android:padding="10dp"
         android:background="#842347"
         android:layout_below="@+id/connect_btn"
         android:layout_toEndOf="@id/guided_mode"
         android:layout_marginStart="15dp"
         android:layout_marginTop="20dp"/>-->

</RelativeLayout>
